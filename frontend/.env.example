# Copy to .env.local for local overrides (optional).
# In dev, leave this empty: Vite proxies "/api" to http://localhost:8080.
# In production (Vercel), set this to the deployed backend origin, e.g.
#   VITE_API_BASE_URL=https://tickr-api.onrender.com
VITE_API_BASE_URL=
